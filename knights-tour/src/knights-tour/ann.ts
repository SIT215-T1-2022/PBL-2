import KnightsTourAlgorithm from "./algorithm";

export default class ANN implements KnightsTourAlgorithm {
  getPath(n: number, startingX: number, startingY: number): [number, number][] {
    throw new Error("Method not implemented.");
  }
}
